# Grafana Service

This directory is reserved for custom Grafana provisioning, plugins, and custom builds.

## Default Deployment
Uses the official Grafana image (`grafana/grafana:11.0.0`) configured in `compose/compose.monitoring.yml`.
Handles dashboard visualization of Prometheus metrics and Loki logs.
