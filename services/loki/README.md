# Grafana Loki Service

This directory is reserved for custom Loki rule configurations or builds.

## Default Deployment
Uses the official Loki image (`grafana/loki:3.0.0`) configured in `compose/compose.monitoring.yml` and mounted to `configs/loki/loki-config.yml`.
