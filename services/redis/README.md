# Redis Cache Service

This directory is reserved for custom Redis configuration builds.

## Default Deployment
Currently uses the official Redis Alpine image (`redis:7.2-alpine`) configured in `compose/compose.database.yml` and mounted to `configs/redis/redis.conf`.
