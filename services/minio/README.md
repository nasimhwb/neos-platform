# MinIO Object Storage Service

This directory is reserved for custom MinIO builds and initialization scripts.

## Default Deployment
Uses the official MinIO server image configured in `compose/compose.storage.yml`.
To set up private buckets, use the standard AWS CLI or MinIO client (`mc`).
