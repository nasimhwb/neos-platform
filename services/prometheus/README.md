# Prometheus Service

This directory is reserved for custom Prometheus rule configurations or custom builds.

## Default Deployment
Uses the official Prometheus image (`prom/prometheus:v2.52.0`) configured in `compose/compose.monitoring.yml` and mounted to `configs/prometheus/prometheus.yml`.
