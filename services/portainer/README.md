# Portainer CE Service

This directory is reserved for custom Portainer builds.

## Default Deployment
Uses the official Portainer CE image (`portainer/portainer-ce:2.20.3-alpine`) configured in `compose/compose.security.yml`.
Handles visual management of VPS container lifecycles safely.
